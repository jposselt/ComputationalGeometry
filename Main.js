function preload() {
    // Load lines from data file
    // EVERYTHING relying on this data must NOT be used here
    // data = loadStrings("");
}

function setup() {
    frameRate(1);
    createCanvas(500, 500);
}

function draw() {
    clear();
}